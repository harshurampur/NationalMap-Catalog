<% if (series==="B") { %> // Currently B01 refers exclusively to Census 2011, but this may change.
  "2011 Census"
<% } else if (series==="T") { %>
  "Over time"
<% } else { %>
  "<%= series %>"
<% } %>